/**
* Holds extra information for the auto generation script
*/
module.exports = {
    //The dependencies that the generated angular module should have
    // ie. if you depend on 'ui-router' and our services
    // dependencies: ['ui.router', 'services']
};
